kind: pipeline
name: default

steps:
  - name: backend
  - name: frontend
  - name: app
  - name: unit-test
  - name: it-test
    volumes:
    - name: dockersock
      path: /var/run

services:
- name: docker
  image: docker:dind
  privileged: true
  volumes:
  - name: dockersock
    path: /var/run

volumes:
- name: dockersock
  temp: {}
